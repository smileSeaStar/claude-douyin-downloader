# 使用说明

## 给同事的分发说明

### 1. 下载方式

**方式一：从 GitHub 下载**
1. 访问项目页面
2. 点击 "Code" 按钮
3. 选择 "Download ZIP"
4. 解压后双击 `run.bat`

**方式二：从 fix-bat-encoding 分支下载**
1. 访问：https://github.com/smileSeaStar/claude-douyin-downloader
2. 切换到 `fix-bat-encoding` 分支
3. 下载 ZIP 文件

### 2. 同事需要做什么

1. **检查是否有 Python**
   - 按 `Win + R`
   - 输入 `cmd` 回车
   - 输入 `python --version`
   - 如果显示版本号，直接跳到第 2 步
   - 如果显示"不是内部或外部命令"，需要安装 Python

2. **安装 Python（如需要）**
   - 访问：https://www.python.org/downloads/
   - 下载 Python 3.8+
   - **重要**：安装时勾选 "Add Python to PATH"
   - 安装完成后，重新打开命令行验证

3. **运行程序**
   - 双击 `run.bat`
   - 等待自动安装依赖
   - 程序自动启动

### 3. 如果 run.bat 打不开

- 右键点击 `run.bat` → "打开方式" → "命令提示符"
- 或者双击 `check_env.bat` 先检测环境

### 4. 文件清单

- `run.bat` - 一键启动程序
- `check_env.bat` - 环境检测工具
- `gui_launcher.py` - 图形界面
- `douyin_downloader.py` - 核心程序
- `requirements.txt` - 依赖列表
- `README.md` - 详细说明
- `使用说明.md` - 本文档
