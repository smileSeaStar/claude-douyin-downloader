# 快速开始指南

## 给同事的一键使用说明

### 第一步：检查是否安装 Python

1. 按 `Win + R` 键
2. 输入 `cmd` 回车
3. 输入 `python --version` 回车

**如果显示版本号**（如 `Python 3.10.0`）：直接跳到第二步  
**如果显示"不是内部或外部命令"**：需要先安装 Python

### 第二步：安装 Python（如需要）

#### 方法一：官方下载（推荐）

1. 访问：https://www.python.org/downloads/
2. 点击 "Download Python 3.x.x"（会自动下载最新版）
3. 运行安装程序
4. **重要**：勾选 "Add Python to PATH"
5. 点击 "Install Now"

#### 方法二：国内镜像（下载更快）

- **官网镜像**：https://www.python.org.cn/download/
- **清华镜像**：https://mirrors.tuna.tsinghua.edu.cn/help/python/
- **下载后**：同样需要勾选 "Add Python to PATH"

#### 验证安装

安装完成后，打开命令行输入：
```
python --version
```
如果显示 `Python 3.x.x` 说明安装成功。

### 第三步：运行程序

1. 双击 `run.bat`
2. 等待自动安装依赖（首次运行需要几分钟）
3. 程序会自动启动图形界面

---

## 如果 run.bat 打不开

### 方法一：使用环境检测脚本

1. 双击 `check_env.bat`
2. 查看检测结果
3. 根据提示操作

### 方法二：手动安装

在文件夹中打开命令行（按住 Shift 右键 → "在此处打开 PowerShell/命令提示符窗口"），运行：

```bash
# 安装依赖
pip install -r requirements.txt

# 安装浏览器
playwright install chromium

# 启动程序
python gui_launcher.py
```

---

## 常见问题

### Q: run.bat 双击后闪退？

A: 可能是 Python 未安装或未添加到 PATH。请运行 `check_env.bat` 查看检测结果。

### Q: run.bat 双击后打开记事本？

A: 这是 Windows 文件关联问题。请右键点击 `run.bat` → "打开方式" → 选择"命令提示符"。

### Q: 依赖安装失败？

A: 检查网络连接，然后运行：
```bash
pip install -r requirements.txt --trusted-host pypi.org --trusted-host files.pythonhosted.org
```

### Q: 需要安装其他软件吗？

A: 不需要！`run.bat` 会自动安装所有需要的依赖。

### Q: 没有 Python 怎么办？

A: 下载地址：
- **官方下载**：https://www.python.org/downloads/
- **国内镜像**：https://www.python.org.cn/download/
- **重要**：安装时务必勾选 "Add Python to PATH"
